// FeaturedCategories - Hidden until category images are uploaded
// This section will be re-enabled once the user provides category banner images

const FeaturedCategories = () => {
  // Section hidden - awaiting user-provided category images
  return null;
};

export default FeaturedCategories;
